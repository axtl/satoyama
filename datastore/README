Start `redis-server` pointing to the `redis.conf` in this folder. It will snapshot to `dump.rdb` every so often.

The format of the data in the store is:

* `next.post.id`
* `post.list`
* `posts:<id>:title`
* `posts:<id>:body`
* `posts:<id>:date`
* `posts:<id>:next.comm.id`
* `posts:<id>:comm:<id>`
* `posts:<id>:comm:<id>:date`
* `posts:<id>:comm.list`
